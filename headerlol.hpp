#include <iostream>
#include <vector>

void primefactorization(std::vector<int> factors, int num);
void primefactorization(std::vector<int> factors, int num, int divisor);
std::vector <int> primefactorization(int num);